# Dependências padrão (detector Haar Cascade — leve, sem downloads pesados).
# Nota: fixado em 4.x — o opencv-python 5.0 (pré-release) não inclui o
# CascadeClassifier necessário para o Haar.
opencv-python>=4.8,<5
plyer>=2.1.0
win10toast>=0.9

# ---------------------------------------------------------------------------
# OPCIONAL: apenas se você quiser usar o backend YOLO (DETECTOR = "yolo").
# Atenção: puxa o PyTorch (~2,5 GB). Instale manualmente se precisar:
#     pip install ultralytics
# ---------------------------------------------------------------------------
